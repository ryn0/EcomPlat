<span>
@model decimal
@{
    int fullStars = (int)Math.Floor(Model);
    for (int i = 1; i <= 5; i++)
    {
        if (i <= fullStars)
        {
            @:★
        }
        else
        {
            @:☆
        }
    }
}
<small class="text-muted">(@Model.ToString("0.0"))</small>
</span>